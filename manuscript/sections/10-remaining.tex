\section{The remaining gaps, exhaustively}\label{sec:remaining}

This section is the paper's ledger: Theorem~\ref{thm:conditional} (rung
ii) is claimed modulo exactly the items below, each stated precisely
where cited. No other unproved ingredient is used anywhere.

\subsection*{On Lemma B (citation-grade; low risk)}
\begin{itemize}
\item[(B1)] \textbf{RESOLVED} (verified against the v2 TeX source of
\cite{TT25}, June 9, 2026): the hypotheses of their Theorem~3.1 are
fully quantitative with absolute constants; the functions enter only
through the equidistribution axiom at scale $X$, so $x$-dependent band
edges are admissible. Their consecutive-smooth theorem states the
analogous uniformity explicitly.
\item[(B2)] \textbf{RESOLVED}: the conclusion is the centered
correlation bound
$\frac WN\sum_{N<n\le2N}(g_1(n+h_1)-\delta_N)g_2(n+h_2)
\mathbf 1_{n\equiv b\,(W)}\ll\mathcal L^{-c}$ at almost all scales,
with $\delta_N$ the mean of $g_1$ up to $O(\mathcal L^{-1})$ --- product
of means follows given the AP-mean of $g_2$ (same B0 input).
\item[(B3)] \textbf{Statement retrieved} (recorded verbatim in the
program's Lemma-B document); the technical condition $g_1(p)=1$ on
$[\exp(\log^{1/11}X),\exp(\log^{1/10}X)]$ holds trivially for our $g_z$.
\emph{Still owed:} the formal write-up of the half-page equidistribution
lemma B0 for $g_z$ (proof sketched: binomial unwinding of $z^{B(n)}$
into band-prime tuples; defect $O(\sqrt N\log N)$).
\end{itemize}

\subsection*{On Lemma C (standard toolbox; medium risk)}
\begin{itemize}
\item[(C1)] The full statement and proof of Lemma~\ref{lem:C}:
Ford--Konyagin's within-side machinery re-run against a
$1$-bounded multiplicative weight on the other side; key inputs
Granville--Shao \cite{GS}, MRT \cite{MRT}; hardest ingredient the
simultaneous-fractional-parts analysis (Friedlander--Iwaniec; the exact
source is to be pinned). Scheduled after the Lemma-D interface
stabilizes.
\end{itemize}

\subsection*{On Lemma D / Estimate D$^\dagger$}
\begin{itemize}
\item[(D1)] Hypothesis~E1 (Family-A wrap-boundary average over
$q\sim Q$).
\item[(D2)] Hypothesis~E2 (Family-B endpoint average over $p\sim P$).
\item[(D3)] The $\ell$-resonance count and Type-I/II ranges of the
anatomy layer, with the Vaughan gluing
(Proposition~\ref{prop:species}; statuses there).
\item[(D4)] The fine-anatomy-class check: Lemma~B classes refine bare
friability; per-class inclusion--exclusion of the same species
(\S\ref{sec:anatomy}, item (iii)).
\item[(D5)] The completion/Vaaler step packaging the binding digit
condition into the harmonics $0<|\lambda|\le\Llog^C$ with the stated
weights, uniformly over slot levels, multi-prime configurations, and
higher exponents $q^e\|n-1$ (deeper characters mod $q^{e+1}$;
bookkeeping of the same shape).
\end{itemize}

\subsection*{On the assembly}
\begin{itemize}
\item[(S1)] Lemma~\ref{lem:margins} (margins are free), within the
Ford--Konyagin framework.
\item[(S2)] Lemma~\ref{lem:A} write-up (one-line adaptation of FK \S2).
\item[(S3)] The cascade bookkeeping of
Proposition~\ref{prop:assembly}: composing A--D with explicit error
budgets at almost all scales; FK \S7 for the constant.
\end{itemize}

\subsection*{Trims (lower-bound track unaffected)}
Top of band $u,u'\le\tfrac12-\eta$; corner
$\sqrt R\ge\Llog^{B/2+c+5}$; margin costs. For
Theorem~\ref{thm:lower} additionally: E1/E2 are not needed
(worst-case-per-modulus versions suffice), and a single
positive-proportion anatomy class suffices.

\subsection*{Known dead ends (recorded; do not re-enter)}
Union bounds across the digit layer (failure mass $\Theta(1)$);
smooth-cofactor designs ($\rho(c/\delta)$ loses to $e^{-c/\delta}$);
the semiprime trick (dies on the prime case); TT/Pilatte as a black box
(dilation step); crude Cauchy--Schwarz over the mod-$q$ family (discards
the deep twist); Heilbronn completion via max-over-intervals (error
dominates); the citation-adaptation route $\alpha+\beta$ for small
$\lambda$ (Remark~\ref{rem:negative}); single-scale (M2) and the
polylog minor claim (Remark~\ref{rem:M2-strengthening}).
