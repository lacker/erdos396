\section{The remaining gaps, exhaustively}\label{sec:remaining}

This section is the paper's ledger: Theorem~\ref{thm:conditional} (rung
ii) is claimed modulo exactly the items below. After the consolidation
of \S\S\ref{sec:typeII}--\ref{sec:bookkeeping}, every remaining item is
either a \emph{citation debt} (a named classical input to be transcribed
verbatim from its source) or a \emph{mechanical write-up}; none is
structural.

\subsection*{Citation debts (classical, sourced, non-structural)}
\begin{itemize}
\item[(SL1)] Exponential sums over primes with hyperbolic phases
(Lemma~\ref{lem:SL1}; Vaughan + van der Corput, \cite[Ch.~13]{IK04}).
Feeds E1 (Proposition~\ref{prop:E1}).
\item[(SL2/SL2$'$)] Linear rational phases over primes / the
$\psi$-over-primes cancellation (Lemmas~\ref{lem:SL2},~\ref{lem:SW};
Vinogradov). Feed E2 and Siegel--Walfisz for $g_{\mathbf z}$.
\item[(DMV)] The quantitative Daboussi--Montgomery--Vaughan bound
(Lemma~\ref{lem:DMV}), uniform over $1$-bounded multiplicative
weights. Feeds the weighted minor pairs of Lemma~C.
\item[(Hal)] A Hal\'asz--MRT-type mean-value input for the
major-pair absorption (remark after
Proposition~\ref{prop:C-weighted}).
\item[(TT)] \cite{TT25}, Theorem~3.1 --- statement verified against
the v2 source; transcription into the final manuscript pending.
\item[(GS/DGS)] \cite{GS} and the smooth-supported extension
(Drappeau--Granville--Shao) at the stated ranges.
\item[(Vin)] Primes in progressions against linear phases for the
extreme anatomy range (Lemma~\ref{lem:extreme}).
\end{itemize}

\subsection*{Mechanical write-ups}
\begin{itemize}
\item[(W1)] Uniformity bookkeeping in $(q,\mu,p)$ when summing the
three anatomy regimes (Proposition~\ref{prop:glue}); the
$s$-aggregation in the linear-resonance average (remark after
Lemma~\ref{lem:dilated}).
\item[(W2)] The fine-class inclusion--exclusion bookkeeping
(\S\ref{sec:anatomy}, item (iii); structure verified, WP2.4).
\item[(W3)] The FK gluing for Lemma C: carrying the weight through
\cite{FK21} \S\S3--5 with Propositions~\ref{prop:C-cov}--%
\ref{prop:C-single} as inputs.
\item[(W4)] The Dickman--Billingsley identification of the band sums
$\Sigma_{\mathbf j}$ with the box measures
(Proposition~\ref{prop:B-assembled}).
\item[(W5)] Final manuscript assembly: theorem statements
re-verified against the composed error budget of
Proposition~\ref{prop:cascade}.
\end{itemize}

\subsection*{Status summary}
\begin{center}
\begin{tabular}{ll}
\toprule
Piece & Status after this consolidation\\
\midrule
Lemma 0, margins (0$'$) & \statusP{} (\S\ref{sec:sandwich},
\S\ref{sec:bookkeeping})\\
Lemma A & \statusP{} (\S\ref{sec:bookkeeping})\\
Lemma B & \statusP{} modulo (TT), (SL2$'$), (W4)\\
Lemma C & reduced; \statusP{} modulo (DMV), (Hal), (GS/DGS), (W3)\\
D$^\dagger$: deep harmonics & \statusP\\
D$^\dagger$: digit, minor & \statusP\\
D$^\dagger$: digit, major (E1, E2) & \statusP{} modulo (SL1), (SL2)\\
D$^\dagger$: anatomy ranges & glued; \statusP{} modulo (Vin), (W1),
(W2)\\
Depth packaging (D5) & \statusP{} (\S\ref{sec:bookkeeping})\\
Assembly cascade & \statusP{} given A--D
(Proposition~\ref{prop:cascade})\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{Trims (lower-bound track unaffected)}
Top of band $u,u'\le\tfrac12-\eta$; corner
$\sqrt R\ge\Llog^{B/2+c+5}$; slot-boundary slivers $O(\varepsilon')$;
margin costs (now proved free). For Theorem~\ref{thm:lower}: E1/E2 are
not needed (worst-case-per-modulus versions suffice), and a single
positive-proportion anatomy class suffices.

\subsection*{Known dead ends (recorded; do not re-enter)}
Union bounds across the digit layer (failure mass $\Theta(1)$);
smooth-cofactor designs ($\rho(c/\delta)$ loses to $e^{-c/\delta}$);
absolute-counting comparisons at any smoothness level (WP5 \S3:
requires $u<2^{1/(k+1)}$ against the forced $u\ge2$);
the semiprime trick; TT/Pilatte as a black box (dilation);
crude Cauchy--Schwarz over the mod-$q$ family;
Heilbronn completion via max-over-intervals;
the citation-adaptation route $\alpha+\beta$
(Remark~\ref{rem:negative}); single-scale (M2) and the polylog minor
claim (Remark~\ref{rem:M2-strengthening}); pure Balog--Wooley plus
CRT forcing (entropy count, WP5 \S5).
